/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DocumentSchemaFooterButtonDirective } from './document-schema-footer-button.directive';

describe('Directive: DocumentSchemaFooterButton', () => {
  it('should create an instance', () => {
    const directive = new DocumentSchemaFooterButtonDirective();
    expect(directive).toBeTruthy();
  });
});